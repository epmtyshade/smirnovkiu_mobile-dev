name: wikipedia
description: A sample command-line application.
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.11.4

dependencies:
  # Add regular dependencies here.

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8